export const metadata = {
  title: 'Browse',
  description: 'Browse approved lost and found items across Jazeera University campus.',
};

export default function BrowseLayout({ children }) {
  return children;
}
