export const metadata = {
  title: 'How it works',
  description: 'Learn how JU LOFO reports are reviewed, published, and returned on campus.',
};

export default function HowItWorksLayout({ children }) {
  return children;
}
