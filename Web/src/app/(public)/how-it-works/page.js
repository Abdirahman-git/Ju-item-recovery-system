'use client';

import ProcessZigzag from '@/components/public/ProcessZigzag';

export default function HowItWorksPage() {
  // Match the “look deeply” W-path / circular workflow design.
  // (Keeps the page consistent across breakpoints via `globals.css`.)
  return <ProcessZigzag />;
}
